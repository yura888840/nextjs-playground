// Shared with the form for early feedback. The server independently enforces every limit.
export const maxAttachmentBytes = 1024 * 1024;
export const maxAttachmentsPerTask = 10;
export const maxAccountAttachmentBytes = 20 * 1024 * 1024;
export const attachmentAccept = '.txt,.pdf,.png,.jpg,.jpeg';
